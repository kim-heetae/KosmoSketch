<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace = "hit.login">
	<select id="proc_login" parameterType="yj.LoginDTO" statementType="CALLABLE">
	{call proc_Login(#{user_Id, mode=IN, jdbcType=VARCHAR, javaType=String},#{password, mode=IN, jdbcType=VARCHAR, javaType=String},
	#{result, mode=out, jdbcType=VARCHAR, javaType=String})} 
	</select>
</mapper>